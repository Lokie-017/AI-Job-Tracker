# AI Job Tracker Agent (Streamlit)

Run the Streamlit prototype locally:

```bash
python -m venv .venv
.\.venv\Scripts\Activate.ps1
pip install -r requirements.txt
streamlit run streamlit_app.py
```

This prototype contains skeleton UI components for the AI Job Tracker Agent.
